##############################################################
# Managed by Puppet. Do not edit. Changes will be wiped out. #
##############################################################

# Catch all vhost

<VirtualHost _default_:*>

	ServerAdmin root@localhost
	DocumentRoot /var/www/default/public
	
	# Always block file system root access in /var/www/default and higher
	
	<Directory /var/www/default>
		AllowOverride None
		Options None
		Order Deny,allow
		Deny from all
	</Directory>
	
	# Sets access, only in /var/www/default/public
	
	<Directory /var/www/default/public>
		AllowOverride None
		Options None
		Allow from all
		Order Allow,deny

	</Directory>

	ErrorLog ${APACHE_LOG_DIR}/error.log

	LogLevel warn

	CustomLog ${APACHE_LOG_DIR}/access.log combined
	
</VirtualHost>

# Localhost catch vhost

<VirtualHost *:80>
	
	ServerAdmin root@localhost
	DocumentRoot /var/www/default/public
	
	ServerName localhost
	
	# Always block file system root access in /var/www/default and higher
	
	<Directory /var/www/default>
		AllowOverride None
		Options None
		Order Deny,allow
		Deny from all
	</Directory>
	
	# Sets access, only in /var/www/default/public
	
	<Directory /var/www/default/public>
		AllowOverride None
		Options None
		Allow from all
		Order Allow,deny

	</Directory>

	ErrorLog ${APACHE_LOG_DIR}/error.log

	LogLevel warn

	CustomLog ${APACHE_LOG_DIR}/access.log combined
	
</VirtualHost>


# FQDN catch vhost

<VirtualHost *:80>
	ServerAdmin root@localhost
	DocumentRoot /var/www/default/public
	
	ServerName <%= default_fqdn %>
	
	# Always block file system root access in /var/www/default and higher
	
	<Directory /var/www/default>
		AllowOverride None
		Options None
		Order Deny,allow
		Deny from all
	</Directory>
	
	# Sets access, only in /var/www/default/public
	
	<Directory /var/www/default/public>
		AllowOverride None
		Options None
		Allow from all
		Order Allow,deny

	</Directory>

	ErrorLog ${APACHE_LOG_DIR}/error.log

	LogLevel warn

	CustomLog ${APACHE_LOG_DIR}/access.log combined
	
</VirtualHost>

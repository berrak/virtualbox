##############################################################
# Managed by Puppet. Do not edit. Changes will be wiped out. # 
##############################################################
127.0.0.1	localhost
<%= vaddress %>   <%= vhostname %>.<%= vdomain %>  <%= vhostname %>  node-<%= vhostname %>

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

# Source in any other virtual hosts snippets for apache2

cd /etc/vhosts.d
for vhosts in *.vh
do
    source $vhosts
done
cd /
